import 'package:flutter/material.dart';

class ServicesTransactionsWidget extends StatelessWidget {
  const ServicesTransactionsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('PaymentWidget');
  }
}
