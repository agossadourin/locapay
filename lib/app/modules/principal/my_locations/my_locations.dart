import 'package:flutter/material.dart';

class MyLocations extends StatelessWidget {
  const MyLocations({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('MyLocations');
  }
}
